# Ensure the cosmic-greeter system user home directory exists before
# cosmic-greeter.service starts. /var is not part of the immutable image
# layer, so this can't be pre-created at build time — tmpfiles handles it.
d /var/home/cosmic-greeter 0750 cosmic-greeter cosmic-greeter -