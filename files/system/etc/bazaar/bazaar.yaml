yaml-blocklist-paths:
  - /run/host/etc/bazaar/blocklist.yaml

curated-config-paths:
  - /run/host/etc/bazaar/config.yaml

override-eol-markings:
  - com.obsproject.Studio

hooks:
  - id: welcome
    when: before-transaction
    dialogs:
      - id: welcome-dialog
        title: "Welcome to bog!"
        body: ":))))))))))))))"