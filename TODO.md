* Address multiple uses of a Fedora major version number throughout the workflows and attempt to reduce so the installer to the end-system is using the same version of Fedora, just a peace of mind thing.
* I'd like to add some kind of Markdown-based wiki (static files/libraries like Bootstrap/some templating [javascript based? php based?] for presentation and a .desktop file in the menu that opens to an index of it all) for random odds and ends when running the system, things like BTRFS filesystem configuration preferences, where/how to mount secondary drives, etc] and what/how to maintain a system. Just general things I'd like to make particular note of so people don't have to worry about simple things (For example, I got two drives and I want to install it proper for use in Steam, I open the Disks app and do what I think needs to be done for it to show up in the client, that's where something like this would come in, another one: I regularly open images from my local system in my flatpak firefox so on a new system I gotta enable filesystem access to that folder/device, checking for automount too may need to be in among the recommendations)
* With regards to the build-iso workflow, I'd like to reassess the general write-up and see how much I could rely on Blue-Build-made modules and less on bash scripting, just generally trying to be more human-readable and hopefully more stand-out and debuggable in the workflow logs.
* Readdress generic CEC support
* Readdress opt-in cloud LLM-through-CLI-based crash troubleshooting feature like Omarchy.