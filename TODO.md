* Some kind of SearXNG (or other self-hostable search engine) install (optional to enable similar to the Jackett option)
* Pre-configuration to add the search engine install as a search option in the pre-installed Firefox Flatpak